* 因为进程有独立性，所以如何通信（IPC）？

  **前提：让不同的进程看到同一份资源**->某种形式的内存空间->提供资源的人只能是OS

  ![image-20260114093739986](https://gitee.com/binary-whispers/pic/raw/master///20260114093742322.png)

* 本地通信：同一台主机，同一个OS，不同进程之间通信---systemV



管道：古老的通信方式

注意：磁盘上的文件加载到内存中，多个进程都能看到，创建子进程时，不用重新拷贝文件内容了！只用拷贝父进程的PCB、页表！

`who | wc -l` 两个进程



# 匿名管道

和inode关联不大 可以不考虑

![image-20260114100346687](https://gitee.com/binary-whispers/pic/raw/master///20260114100348746.png)

可以设计一个纯内存级别的文件->管道

**管道只能单向通信**

操作完最好关闭读写端！

不关闭呢？fd泄漏、容易误操作

**必须先创建管道，再创建子进程**->子进程先创建，看不到文件了

父进程必须以rw方式打开，子进程可以继承rw，然后选择到底是r还是w

管道不需要路径，所以没名字



## 接口

`pipe`

vscode `ctrl+/`批量注释、取消注释

 重定向完整写法`fd 1> fd2`fd2写到fd1里

![image-20260114103645029](https://gitee.com/binary-whispers/pic/raw/master///20260114103646782.png)

把标准输出和标准错误分离到不同文件，就可以更好的debug

创建的管道`fd[2]`

一般来说`fd[0]->r`  `fd[1]->w`



对于共享资源，会出现数据不一致问题（我还没写完，就读取了）

需要保护共享资源->临界资源   管道内部自己做了保护

父进程在没有数据的时候，会阻塞等待



现象：

> * 管道为空&&管道正常，`read`会阻塞
>
>   ![image-20260114114508353](https://gitee.com/binary-whispers/pic/raw/master///20260114114510652.png)
>
>   Ubuntu管道大小64KB
>
> * 管道为满&&管道正常，`write`会阻塞
>
> * 管道写端关闭&&读端继续，读端读到0，表示读到文件结尾
>
> * 管道写端正常&&读端关闭，os会直接杀掉写入的进程
>
>   * OS会给目标进程发送信号 13`SIGPIPE`
>
>   * 怎么证明：
>
>     获取退出信号



特性：

>* 面向字节流---->不太理解，todo 按照字节大小为单位读取
>* 用来进行具有血缘关系的进程通信，常用于父子
>* 文件的生命周期随进程，管道也是
>* 管道只用来单向数据通信->双向通信->俩管道
>* 管道自带同步互斥等保护机制

## 进程池

![image-20260114143818959](https://gitee.com/binary-whispers/pic/raw/master///20260114143821144.png)

### makefile

![image-20260114145224535](https://gitee.com/binary-whispers/pic/raw/master///20260114145229029.png)

clean自己写



进程池有个bug 画图解释  ---->解决bug可以倒序退出进程

---->`channels`保存的是上一个进程的写端，子进程运行之前，可以先把上一个写端关了



匿名管道是子进程继承父进程资源，让父子进程看到的





# 命名管道->任意进程通信

`mkfifo`

`unlink`

![image-20260115102357061](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115102357061.png)

命名管道是真正存在的文件->存在路径+文件名->有唯一性

再来一次：让不同进程看到同一份资源->让不同进程，用同一个文件系统路径，标志同一个资源

p开头就是命名管道文件，只用内核文件缓冲区就行 不做磁盘刷新就行



## 代码

### 预备：形成两个可执行

![image-20260115105605531](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115105605531.png)

对于公共资源，要有特定的进程先行创建

一个创建&&使用资源

一个获取&&使用资源





# system V共享内存

在共享区映射到一块空间，记录内存空间的虚拟起始地址，两个进程都是如此，这样就可以通信了

不用共享内存时，去关联->把页表映射关系去掉就行

共享内存可以在OS内部同时存在很多份->管理->先描述，再组织

创建&&使用

获取&&使用



`shmget`创建共享内存

![image-20260115154214711](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115154214711.png)

存在返回，获取---->保证调用进程能拿到共享内存

存在出错返回---->只要成功，一定是新的共享内存，不拿老的共享内存

flag是位图



`key`是啥？

在参数位置，必须用户输入，必须要标识shm唯一性，怎么保证不同进程看到的是同一个共享内存？

这个key为什么要用户传入？为什么内核不生成？

![image-20260115155658375](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115155658375.png)

内核提供的话，保证不了看到同一份共享内存

![image-20260115155946157](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115155946157.png)

共享内存的指令

`ipcs -m`查看系统里存在的

`ipcrm -m shmid`删除指定共享内存

> shmid VS key
>
> * shmid：只给用户用的一个标识shm的标识符
> * key：只作为内核中，区分shm唯一性的标识符，不作为用户管理shm的id值
>
> 注意：指令也是进程！

代码释放：

`shmctl`

把共享内存挂接到地址空间：`shmat`

![image-20260115170530737](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115170530737.png)

监控脚本



![image-20260115173018548](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115173018548.png)







### 共享内存特点

* 通信速度最快

* 让两个进程在各自的用户空间共享内存，但是没有保护机制

* 共享内存，保护机制需要用户自己完成--->信号量---->可以用管道

  共享资源变成保护资源叫临界资源（枷锁了）

  ![image-20260115192927835](C:/Users/hp/AppData/Roaming/Typora/typora-user-images/image-20260115192927835.png)







