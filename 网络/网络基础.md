* @[toc]
  # 1. 网络的发展史
  
  一张图解释：

  ![image-20260131161651338](https://i-blog.csdnimg.cn/img_convert/b1dcf370afebbb8ee3d0614c53c2c0b3.png)
  
  # 2. 认识协议
  
  ## 网络协议定义
  
  一句话：**网络协议是双方认可的结构体**：
  
  * 底层视角：
  
    网线/光纤里跑的是0/1比特流。两台机器间通信，要规定好比特流特定位代表什么
  
  * 代码视角：一段连续的内存布局规则
  
    ```cpp
    struct TCPHeader {
        uint16_t src_port;  // 源端口 (2字节)
        uint16_t dst_port;  // 目的端口 (2字节)
        uint32_t seq_num;   // 序列号 (4字节)
        uint32_t ack_num;   // 确认号 (4字节)
        // ... 其他字段
    };
    
    // 发送时：把 struct 强转成 char* 发出去
    send(sockfd, (char*)&my_header, sizeof(my_header), 0);
    
    // 接收时：把收到的 char* 强转回 struct 读取
    struct TCPHeader* header = (struct TCPHeader*)recv_buffer;
    ```
  
  ## 网络分层
  
  ### OSI七层模型
  
  ![image-20260131163116853](https://i-blog.csdnimg.cn/img_convert/1b04a8e7ece81e7419f2d889962f7463.png)
  
  ### TCP/IP五层模型
  
  TCP/IP包括很多协议，是一个协议族
  
  ![image-20260131163920426](https://i-blog.csdnimg.cn/img_convert/bf010ab29c0463b428f0513e9eb916f4.png)
  
  下图是TCP/IP模型和OSI模型之间的映射关系，在工程中，使用五层模型
  
  ![image-20260131164447318](https://i-blog.csdnimg.cn/img_convert/b29cf83fd5dfd22891a3bbba54e0f4f5.png)
  
  **为什么要有TCP/IP协议族？**
  
  1. **为了解决异构系统的通信障碍**，世界上存在不同的硬件厂商，各自底层标准不同，TCP/IP制定了一份通用的语言，使得不同硬件可以通信
  
  2. **为了解决长距离通信的不可靠性**，与TCP/IP 协议族通过 IP 地址解决定位问题，通过 TCP 协议解决可靠性问题，本质上是一套应对复杂网络环境的解决方案
  
  3. **为了屏蔽底层物理差异**，它在应用层和物理硬件之间构建了一个抽象层，让开发者不需要关心底层是光纤还是 WiFi，实现了全球网络的逻辑统一
  
  # 3. 网络传输基本流程
  
  ## 局域网网络传输（以太网为例）
  
  ### MAC地址
  
  **MAC**地址用来识别数据链路层中相连的节点，并且**标识主机的唯一性**
  
  长度位`48`比特位，一般用`16`进制数字加冒号形式表示
  
  ### 通信方式
  
  原理类似开会，会议室中只允许一个人讲话，一个人讲话所有人都能听到；若多人讲话，则会发生碰撞
  
  **正常通信：单车道畅通**
  
  在早期的以太网中，所有人都连在同一根线上（集线器 Hub 模式），就像大家都在同一个会议室里 
  
  * **规则**：任何时刻，只允许一个人说话（发数据） 
  * **流程**：主机 A 想发数据，先听一下线路上有没有人在发。如果安静，它就发
  * **广播特性**：A 发出的声音，B、C、D 都能听见，但只有目标 B 会接收，其他人丢弃（根据 MAC 地址）
  
  ![image-20260131170317198](https://i-blog.csdnimg.cn/img_convert/4b91455fc3d32720022102fce236b784.png)
  
  **发生碰撞：车祸现场**
  
  如果 A 和 C 同时也想发数据，并且都觉得刚才没人说话，于是同时开口，这就叫**数据碰撞（Collision）** 
  
  这时候物理信号（电压）会在电缆上叠加，变成乱码，谁也听不懂
  
  ![image-20260131170518655](https://i-blog.csdnimg.cn/img_convert/bfeb83402c2e54414743b6c25b1f150a.png)
  
  以上就是局域网通信原理
  
  ### 同一网段内两台主机发消息的过程
  
  这里我们省去物理层
  
  在同一个网段（比如都在家里的 WiFi 下），主机 A 发给主机 B 一个“你好”，不需要经过路由器转发，直接通过**交换机**（或直连）进行 MAC 寻址。
  
  这个过程就是一次**自顶向下的“封装”和自底向上的“解包”**
  
  ![image-20260131171609924](https://i-blog.csdnimg.cn/img_convert/d75b66b56d3025c31e7b0476566f01f9.png)
  
  **概念拆解：报头 VS 载荷 VS 报文：**
  
  利用快递信封这个例子理解：
  
  * 报文：整体，报文=报头+荷载，“信封+信纸”
  * 报头：信封，信封上写着寄件人、收件人、邮票等**控制信息**
  * 有效载荷：信纸，上层用户想真正传递的内容
  
  **上一层的报文，就是下一层的有效荷载**
  
  * 对于TCP层
    * 报头：TCP头（端口号、序号）
    * 载荷：应用层数据（HTTP头+网页内容）
    * 报文：TCP头+应用层数据
  * 对于IP层
    * 报头：IP头（IP地址）
    * 载荷：整个TCP报文->TCP头+应用层数据
    * 报文：IP头+TCP报文
  
  ```txt
  [ 以太网头 [ IP头 [ TCP头 [ 数据 "你好" ] ] ] ]
  |_________||________________________________|
    MAC报头           MAC的有效载荷
  
             |_____||______________________|
              IP报头       IP的有效载荷
  
                    |_______||___________|
                     TCP报头   TCP的载荷
  ```
  
  之后学习协议，就可以在宏观上建立认识：
  
  > 1. 要学习的协议，是如何做到解包的？只有明确了解包，封包也就能理解 
  > 2.  要学习的协议，是如何做到将自己的有效载荷，交付给上层协议的？
  
  ## 跨网络传输
  
  ### IP地址
  
  `IP`有`IPv4`和`IPv6`两个版本，这里主要讨论`IPv4`：
  
  * `IP`地址是在`IP`协议中，**标识网络中不同主机的地址**
  * 对于`IPv4`，`IP`地址是一个`4`字节的整数
  * 通常用**淀粉十进制**字符串表示`IP`地址，例如`192.168.0.1`，用点分割的每一个数字表示一个字节，范围`[0,255]`
  
  **怎么出现两个地址了，怎么区分MAC地址和IP地址？**
  
  > * **IP地址->快递单上的终点收货地址**
  >   * 特点：**全程不变**，它决定了快递的**总体方向**
  > * **MAC地址->运输车辆的车牌号**
  >   * 特点：**每一程都在变**，它决定了快递在**一小段运输区间（局域网）** 交给谁
  >
  > 假设快递从北京到上海，途径济南：
  >
  > ![image-20260131175008831](https://i-blog.csdnimg.cn/img_convert/0f09a748a8e8b7f78963e88988ac36b3.png)
  
  **结论：**
  
  * IP地址（收货地址）：负责**宏观路径规划**
  * MAC（车牌号）：负责**微观局域网传输**
  
  ### 跨网络传输过程
  
  在整个过程中，路由器是最关键的抉择，就像物流中转站一样，负责拆包和打包
  
  假设**主机A`192.168.2.2`**想发数据给**主机B`172.168.2.2`，中间经过 **路由器** ：左手连接 A 的网段 (`192.168.2.1`)，右手连接 B 的网段 (`172.168.2.1`)
  
  ![image-20260131175431952](https://i-blog.csdnimg.cn/img_convert/737d59ad61ad27848fc8863f51b4173c.png)
  
  ## 总结知识点
  
  * IP地址（网络层）
  
    * 定义：IP地址在网络中标识主机的唯一性
    * 作用：提供网络虚拟层，屏蔽底层物理网络差异
    * 特性：在路由转发过程中**IP地址保持不变**
  
  * MAC地址（数据链路层）
  
    * 定义：用来识别数据链路层中相连的节点，网卡出场时确定（物理性唯一）
    * 作用：局域网内定点传输
    * 特性：在跨网段传输中，**MAC地址逐跳改变**
  
  * 路由器（工作在网络层->实现从网络层到物理层的功能）
  
    * 功能：
      * 地址管理
      * 路由选择：规划两台主机之间的数据传输路线
      * 解包和封包：连接不同的数据链路，转换帧格式
  
  * 封装与分用：
  
    * 封装：自顶向下，每层加报头。应用层->TCP段->IP数据段->以太网帧
    * 分用：自底向上：每层去报头，根据报头中的协议字段决定交给上层的谁
  
    